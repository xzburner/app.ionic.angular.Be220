import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic.angular.be220-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
